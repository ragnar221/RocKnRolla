import React from "react";

const PageNotFound = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        marginTop: "6rem",
        fontSize: "2rem",
        textAlign: "center",
        height: "100vh",
        marginBottom: "2rem",
      }}
    >
      PageNotFound Error # 404
    </div>
  );
};

export default PageNotFound;